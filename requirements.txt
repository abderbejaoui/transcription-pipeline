# Web app
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.6

# Correction pipeline
rapidfuzz>=3.0
jellyfish>=1.0

# Speech-to-text (optional but installed by default for the demo)
torch>=2.2
faster-whisper>=1.0
soundfile>=0.12
numpy>=1.24

# Forced alignment
# - ctc-forced-aligner (MahmoudAshraf97 fork): MMS character-level Viterbi
#   alignment, accuracy ~30 ms on Arabic. This is the primary aligner for
#   the /api/transcribe_debug 'play this exact word' UI.
#   IMPORTANT: install from GitHub, NOT PyPI. The PyPI package with the
#   same name is a different project (Deskpai), English-only, and exposes
#   a totally different API.
#     pip install git+https://github.com/MahmoudAshraf97/ctc-forced-aligner.git
# - openai-whisper: fallback for when MMS isn't installed yet. Approximate
#   word timestamps (~200 ms error on Arabic) but no model download needed
#   beyond what whisper already pulls.
openai-whisper>=20231117
# ctc-forced-aligner @ git+https://github.com/MahmoudAshraf97/ctc-forced-aligner.git

# ASR fine-tuning (scripts/finetune_*).
# Pin transformers to the version that matches our qwen-asr wrapper.
# transformers 5.x removes AutoModel and breaks Wav2Vec2ForCTC imports.
transformers==4.57.6
huggingface-hub==0.36.2
datasets>=2.19
accelerate>=0.33
peft>=0.12

# ASR evaluation — industry-standard libraries used by Whisper, HuggingFace
# Open ASR Leaderboard, and NADI 2025 Arabic Shared Task.
jiwer>=3.0
num2words>=0.5.13

# Dataset inspection downloads (Kaggle-hosted free corpora)
kaggle>=1.6
yt-dlp>=2024.8.6
pypdf>=4.0
