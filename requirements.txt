# Web app
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.6

# Correction pipeline
rapidfuzz>=3.0
jellyfish>=1.0

# Speech-to-text (optional but installed by default for the demo)
faster-whisper>=1.0
soundfile>=0.12
numpy>=1.24

# Audio verification (Tier 2)
# transformers + torch are pulled in transitively for the IPA model.
phonemizer>=3.3   # needs system espeak-ng:  brew install espeak-ng
