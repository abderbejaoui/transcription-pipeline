# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Editor / IDE
.vscode/
.idea/
*.swp
.DS_Store

# Project runtime data — voice fingerprints, session audio, LLM caches.
# Each tenant should produce its own.
data/voice_index.npz
data/voice_index.jsonl
data/descriptions.jsonl
data/sessions/
data/dataset_samples/

# Misc
*.log
.coverage
htmlcov/
.pytest_cache/
# Eval audio (~210 MB) and packed prediction archives — too big for git.
# The audio is reproducible from scripts/build_*_testset.py, and predictions
# are kept inside their bakeoff/predictions/ directory (committed separately).
eval/*/audio/
eval/*/wavs/
eval/gulf_medical_v1/wavs/
*.tar.gz
test_gulf*.wav