# DATA PATHS — DGX Spark (authoritative)

All paths are **relative to the repo root on the DGX**:
`/home/abder/abder/transcription/transcription-pipeline`

- Host: `spark-a6f4` (SSH) · venv: `.venv/bin/python` (python3.12) · tmux: `qwen3`
- On the DGX `python` = the venv python. On the **Mac** use `python3` (no `python`).
- Verified on disk 2026-06-06 via `scripts/inspect_train_data.py`,
  `scripts/inspect_synthetic_data.py`, `scripts/find_synthetic_data.py`.

---

## TL;DR — the two finetune inputs (nothing to download)

| Phase | Manifest path | Content |
|---|---|---|
| **Phase 1** (real acoustic) | `data/dgx_full/preprocessed_audios_full/manifest.jsonl` | **804.3h** real Gulf |
| **Phase 2** (synthetic medical) | `data/training/medical_gulf_v2/manifest.jsonl` | **21.01h** synthetic medical code-switch |

Both already exist and are fully preprocessed. **Do NOT re-download or re-prep these.**

---

## 1. Phase-1 real corpus — the "804h" first-finetune data

- **Train manifest:** `data/dgx_full/preprocessed_audios_full/manifest.jsonl`
  - 419.5 MB · **349,242 clips** · **804.3h** (804h 17m 29s; exact via `duration_s`)
  - Schema keys: `id, audio_path, text, duration_s, source_manifest, source_audio, raw_text, trim_start_s, trim_end_s, chars_per_s, source_segment`
- **Rejected (filtered-out) clips:** `data/dgx_full/preprocessed_audios_full/rejected.jsonl`
  - 53.2 MB · 46,724 clips · 63.4h · keep rate **92.7%** (of 867.7h decoded)

### Per-dataset breakdown of the 804.3h (from `source_manifest`)

| Dataset | Clips | Hours | %h |
|---|---|---|---|
| sada2022 | 207,686 | 374.5 | 46.6% |
| worldspeech_kuwait | 93,568 | 155.6 | 19.4% |
| worldspeech_bahrain | 42,189 | 253.7 | 31.5% |
| mixat_emirati | 4,604 | 14.0 | 1.7% |
| worldspeech_saudi | 792 | 5.8 | 0.7% |
| nexdata_uae_sample | 403 | 0.6 | 0.1% |
| **Total** | **349,242** | **804.3h** | 100% |

> SADA, WorldSpeech (KW/BH/SA), mixat, nexdata are **already baked into this one manifest**.
> Do NOT re-prep `sada22`/`worldspeech_*`/`mixat` — that would double-count.
> MASC was NOT part of this corpus.

---

## 2. Phase-2 synthetic medical data

- **Manifest:** `data/training/medical_gulf_v2/manifest.jsonl`
  - **14,920 clips · 21.01h** synthetic medical code-switch (verified: all have wav + transcript)
  - WAVs live in `data/training/medical_gulf_v2/wavs/` (≈6.9 GB), named `med_*.wav` (+ some `code_switching_*.wav`)
  - Schema: `audio` (relative path), `text`, `term`, `type`, `tier`, `duration_s`, `voice_ref`
- Generated by `scripts/synthesize_conversations.py` (TTS runs in a **separate** `~/tts-env` virtualenv).
  - **Do NOT re-synthesize.** It already exists.
- ⚠️ Phase-1 uses `audio_path`; this synthetic manifest uses `audio` — **schema-align before merging into a split**.

### Synthetic decoys (NOT the real set — ignore for training)

| Path | What it is |
|---|---|
| `data/tts_references/` | 10 demo seed clips (`ref_001..ref_010`, 80s) — TTS voice references, not training data |
| `data/tts_preview/` | preview output |
| `eval/gulf_medical_v1/audio/` | ~90 wavs, eval-only synth |

---

## 3. Newly prepared datasets (`scripts/prepare_datasets.py` → `data/preprocessed/<slug>/`)

Each slug folder has `manifest.jsonl` + `summary.json` (+ clips). Status as of 2026-06-07:

| Slug | HF source | Clips | Hours | Dialect | Stage | Weight | Use |
|---|---|---|---|---|---|---|---|
| `masc` | `MohamedRashad/MASC-Arabic` (parquet, `type='c'` clean only) | 358,367 | ~393 | msa/mixed | 1 | 0.7 | train |
| `saudi_asrv1` | (saudi ASR v1) | 6,598 | ~86 | saudi | 1 | — | train |
| `common_voice_ar` | `Geethuzzz/common_voice_17_0_arabic_cleaned` (parquet, split `validated`) | 78,156 | 88.6 | msa | 1 | 0.3 | train |
| `scc22` | `MohamedRashad/SCC22` | 3,296 | ~4.5 | saudi | 2 | — | **eval-only** |
| `casablanca` | (Casablanca) | 1,626 | ~1.9 | mixed | 2 | — | **eval-only** |

Paths: `data/preprocessed/masc/manifest.jsonl`, `.../saudi_asrv1/...`,
`.../common_voice_ar/...`, `.../scc22/...`, `.../casablanca/...`.

> `common_voice_ar` prepped OK on 2026-06-07: **78,156 clips / 88.6h** (the
> cleaned mirror dedups/filters, so fewer than the raw ~157h estimate). The
> mozilla-foundation script-based repo had failed on `datasets>=3.x`; fixed in
> commit `7da44e8` by switching to the Geethuzzz parquet mirror.

### Dropped / skipped (do not chase)

- **Gated, no auto-download:** `ramsa` (~41h Emirati), `zaebuc` (~12h CS), `arzen` (~12h Egyptian CS), `oman_speech` (paper-only).
- **Quality-audited out (2026-06-06):** `emirati_shows` (~0.5–1h, broken tsv join), `sawtarabi` (no card, unverifiable).

---

## 4. Pipeline / split outputs (built locally on DGX, no network)

| Path | What |
|---|---|
| `data/splits/phase1.train.jsonl` / `phase1.val.jsonl` | Phase-1 train/val split (from `scripts/split_manifest.py`) |
| `data/splits/phase2_rehearsal.jsonl` | ~100h carved Gulf rehearsal pool for Phase 2 |
| `runs/smoke/` | A4 smoke-test run output |

> A3 split command lives in `FINETUNE_RUNBOOK.md` (Part A). The `--in` for Phase 1
> is **`data/dgx_full/preprocessed_audios_full/manifest.jsonl`** (the 804h corpus above).

---

## 5. Helper inspection scripts

| Script | Purpose |
|---|---|
| `scripts/inspect_train_data.py` | per-dataset breakdown of the 804h train manifest |
| `scripts/inspect_synthetic_data.py` | count/measure synthetic clips (`--root <dir> --index manifest.jsonl`) |
| `scripts/find_synthetic_data.py` | hunt for synthetic audio anywhere under a root |
| `scripts/prepare_datasets.py` | dataset registry + prep → `data/preprocessed/<slug>/` |
| `scripts/prep_new_datasets.sh` | overnight runner for the new datasets |
| `scripts/split_manifest.py` | build phase1 train/val + carve phase2 rehearsal (`leakage=0`) |
| `scripts/finetune_qwen3_lora.py` | the LoRA finetune script (`python -m scripts.finetune_qwen3_lora`) |
